# G6 交付汇总 · vynth AICoding 全量架构方案合稿裁决书

> 主理人（AICoding 架构专家团 lead）统一合稿说明。本文件不替代任何成员专业产出，仅做术语统一、版本/引用一致性核对与冲突裁决归集，供 G6 最终审核与归档使用。

## 一、产物清单与路径

| # | 文档 | 阶段 Gate | 自动校验 | 输出路径 |
|---|---|---|---|---|
| 1 | 项目资料摘要（material_digest） | G1 ✅ | 7/7 | `.workbuddy/output/material_digest.md` |
| 2 | 行业调研报告（research_report） | G2 ✅ | 12/12 | `.workbuddy/output/research_report.md` |
| 3 | 高层架构设计 | G3 ✅ | 12/12 | `.workbuddy/output/高层架构设计.md` |
| 4 | 系统设计 | G4 ✅ | 11/11 | `.workbuddy/output/系统设计.md` |
| 5 | UserStory | G4 ✅ | 5/5 | `.workbuddy/output/UserStory.md` |
| 6 | 部署设计 | G5 ✅ | 8/8 | `.workbuddy/output/部署设计.md` |
| 7 | 安全设计 | G5 ✅ | 9/9 | `.workbuddy/output/安全设计.md` |

## 二、术语统一结果

| 术语 | 统一口径 | 落点文档 |
|---|---|---|
| 产品定位 | 本地优先单二进制 TUI 编程工具（Vibe Coding），非云 SaaS / 非多租户 | 全部 5 份主文档 |
| LLM 默认端点 | `https://api.deepseek.com/v1`（代码为准，冻结 X1） | 高层/系统/部署/安全/UserStory |
| 默认模型 | `deepseek-chat`（代码为准，冻结 X2） | 同上 |
| 沙箱策略 | MVP 软隔离（设计信任模型 `VYNTH_NET` 出站开关）；OS 级硬隔离（bubblewrap/seatbelt）推迟至完整版 F15（G3 方案 A 裁决） | 高层/系统/部署/安全/UserStory |
| 路径守卫 | `safeResolve` 规范化解析 + `assertSafeEndpoint` 出站校验，对抗 symlink/路径穿越（X3 sandbox bug 已修复，作为对抗项） | 安全/部署/系统 |
| 插件接入 | `--plugin` CLI 无头接入已落地（冻结 X4） | 高层/系统/UserStory |
| workspace 声明 | 双声明（README + 代码）历史兼容，无害（冻结 X5） | 高层/系统 |
| token 时延 SLA | MVP 不承诺 P50/P90/P99，仅冷启动 P95 ≤ 150ms（N1）硬指标 + 定性「流式不卡顿」（G4 [中间确认-1] 方案 A 裁决） | UserStory §6.2 / 系统 |

> 注：G6 合稿核对确认上述术语在 5 份主文档中引用一致，无口径打架。

## 三、冲突裁决清单（X1–X7，源自 material_digest）

| 编号 | 冲突点 | 裁决 | 裁决阶段 |
|---|---|---|---|
| X1 | 默认 LLM 端点：代码 `api.deepseek.com/v1` vs 文档 `openai.com/v1` | 以代码为准（DeepSeek） | G1 冻结 → G3 继承 |
| X2 | 默认模型：`deepseek-chat` vs 文档 `gpt-4o-mini` | 以代码为准 | 同上 |
| X3 | sandbox 符号链接越界 | 代码已修复，作为安全设计对抗项 | 同上 |
| X4 | plugin CLI 加载状态：文档"未合并" vs 代码"已实现无头" | 以代码为准（已实现） | 同上 |
| X5 | workspace 双声明 | 历史兼容，无害 | 同上 |
| X6 | README env 变量完整度（仅 4 个 vs 代码更多） | 文档口径差异，标注待补；不影响架构 | G1 标注 |
| X7 | 二进制体积：≤61MB（当前）vs ≤40MB（目标） | 当前基线 + 完整版目标，均已纳入 N1/N2 跟踪 | 同上 |

## 四、方案分歧裁决链（中间确认协议追溯）

| 阶段 | 发起方 | 论题 | 裁决 | 落点 |
|---|---|---|---|---|
| G3 [附录 C.2] | business-architect（xu） | MVP 沙箱范围：推迟 vs MVP 即做 OS 级硬隔离 | **方案 A：推迟至完整版 F15** | 高层架构 §4.3/§6.1/§6.3 |
| G4 [中间确认-1] | product-story-designer（gu） | §6.2 token 级时延 SLA 是否承诺 | **方案 A：不承诺，仅冷启动 P95 硬指标** | UserStory §6.2 / §7.1 / 附录 A.4 |

> 两处分歧均经主理人按协议以 AskUserQuestion 单题弹窗转交用户裁决，原文回注成员，未静默选择。G5 双方自检均未命中 §2.1/§2.2，无新增待裁决项。

## 五、跨阶段一致性核对（G5 交叉 diff 本地适配版）

| 检查项 | 部署侧 | 安全侧 | 结论 |
|---|---|---|---|
| 信任域命名 | 进程域/文件系统域/外联域/插件域 | Z1 进程/Z2 文件/Z3 网络出站/Z4 插件 | ✅ 一致 |
| egress（`VYNTH_NET`） | 仅 `api.deepseek.com:443` HTTPS | 白名单仅 `api.deepseek.com`，默认开 | ✅ 一致 |
| 密钥存储机制 | `secret:env-apikey`(MVP)/`secret:os-keychain`(完整版) | OS keychain / AES-256-GCM 加密配置 | ✅ 一致 |
| 密钥分级 | 引用安全侧 L1–L3 | 5 类（API Key/AKSK/插件 token/口令/外部 Key）权威 | ✅ 一致 |
| 审计保留期 | `≥1 年（合规）` | `≥1 年` | ✅ 部署侧≥安全侧 |
| 注入防护层（WAF 类比） | `assertSafeEndpoint`+`safeResolve`+`VYNTH_NET` | 提示注入过滤+系统提示完整性 | ✅ 一致 |

## 六、版本与引用一致性

- 高层架构设计：v0.1（G3 审核通过基线），下游系统/UserStory/部署/安全均回溯其 §章节，无漂移。
- 功能清单 F1–F15：高层架构 §6.3 ↔ UserStory §3 逐行互查一致，MVP/完整版归属未改。
- 引用溯源：各文档 `§章节` 引用 material_digest 的 D 编号与 research_report 的 B/SR 编号，链路可追溯。

## 七、待办与开放项（移交后续）

- U-02 单二进制体积 20–40MB（当前 ≤61MB，完整版目标）。
- U-03 OS 级沙箱（F15，完整版）。
- U-05 MCP 协议版本（F12，尚未合入 CLI）。
- O1–O5 边界（MCP CLI / TUI 内插件 / 配置合规层 / OS 沙箱 / 多租户 SaaS）均 Out-of-Scope 至完整版，不在本期 MVP 设计范围。

## 八、G6 归档动作（待审核通过后执行）

审核通过后，将以上 7 份文档（含本汇总）复制至 `delivery/`，并写入统一版本戳 `v0.1-final`（G6 归档态）。
